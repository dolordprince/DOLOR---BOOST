# JWT Secret for authentication
JWT_SECRET=your_super_secret_jwt_key_here
# AI Monitor check interval in milliseconds (5 minutes)
AI_CHECK_INTERVAL=300000
# Stalled order threshold in milliseconds (1 hour)
STALLED_ORDER_THRESHOLD=3600000
# Failure rate threshold for provider health alerts
FAILURE_RATE_THRESHOLD=0.15
